apiVersion: v1
kind: ResourceQuota
metadata:
  name: group-quota
  namespace: group01
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "4"
    requests.storage: 10Gi
